export type HostOptionsType = {
  profile: string;
  id: string;
}[];

export type PlatformOptionsType = {
  label: string;
  value: string;
}[];

export type ConfigDropdownOptions = {
  display: string;
  value: string;
  signup_url?: string;
  description?: string;
};
