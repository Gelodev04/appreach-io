import { HostCreateView } from 'src/sections/host/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Register a new infrastructure | Outreach Magic',
};

export default function HostsCreatePage() {
  return <HostCreateView />;
}
