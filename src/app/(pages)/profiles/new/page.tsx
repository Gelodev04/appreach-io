import { SenderCreateView } from 'src/sections/host/view';

export const metadata = {
  title: 'Register a new sender profile | Inbox Daddy',
};

export default function HostsCreatePage() {
  return <SenderCreateView />;
}
